/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['i.postimg.cc'],
        unoptimized: true,
    },
    optimizeFonts: false,
}

module.exports = nextConfig
